const accountName="Permanant";

let accountId = " Idef9";

var accountType="domestic";

accountCity="bangalore";
    let accountDist;

// we have to reduce the usgae of var it is of no use we can use const and let //
//const && let//

//to print the whole things all together we can use consple.table instead of console.log//
// it can create the table of the output//

// if we do not declare any type the value in the memeory remains undefined//
console.table([accountId,accountName, accountType , accountCity , accountDist]);
console.log(accountId);